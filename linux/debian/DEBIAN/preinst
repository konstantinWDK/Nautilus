#!/bin/bash
# preinst script para nautilus-vscode-widget v3.3.8
# Simplificado y optimizado

set -e

case "$1" in
    install|upgrade)
        echo "Preparando instalación de Nautilus VSCode Widget v3.3.8..."
        
        # Solo mostrar mensaje sin intentar detener procesos
        # Los procesos se detendrán automáticamente durante la actualización
        ;;
esac

exit 0
